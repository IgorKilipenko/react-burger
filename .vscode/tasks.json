{
  "version": "2.0.0",
  "tasks": [
      {
          "label": "build",
          "command": "npm run build",
          "type": "shell",
          "args": [
            "NODE_OPTIONS=--openssl-legacy-provider"
          ],
          "problemMatcher": "$msCompile"
      }
  ]
}